{
  "name": "auth-service",
  "version": "1.0.0",
  "author": "Franck Kabasele",
  "scripts": {
    "deploy": "sls deploy -v --stage dev",
    "deployjs": "sls deploy -f createAuction -v",
    "invoke": "sls invoke -f processAuctions -l"
  },
  "description": "Demonstration of protecting API gateway endpoints with auth0",
  "license": "MIT",
  "dependencies": {
    "jsonwebtoken": "^8.1.0"
  },
  "devDependencies": {
    "serverless-bundle": "^1.3.3"
  }
}